{
  "name": "mcp-image-server",
  "version": "1.0.0",
  "main": "dist/image-resolver.js",
  "scripts": {
    "build": "npx -p typescript tsc",
    "start": "node dist/image-resolver.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "MCP Image Viewer: An MCP server designed to resolve and provide image data for display within the chat context, enabling visual elements like emotion expression.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "sharp": "^0.34.1",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4",
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.3"
  }
}
